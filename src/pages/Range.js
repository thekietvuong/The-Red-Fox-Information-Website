import React from "react"
import Model from "./Model";
import RangeImg from "../img/Range.png"
function Range(){
    return(
        <Model title = {"Range"} img = {RangeImg} 
            paragraph1 = {"Red foxes can be found throughout the continental United States from Alaska to Florida. The smallest population is in the Southwest, where it is very rare to see a red fox. Red foxes like open areas in woodlands, rural and suburban neighborhoods, wetlands, and brushy fields."}
        />
  	);
}
export default Range;
